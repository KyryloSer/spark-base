# spark-base

### Check JDK Installation
```java -version```

### Create virtual environment
```python -m venv venv```

### Install Dependencies

To install all required Python packages, run the following command in the project directory:

```pip install -r requirements.txt```

### Run Application with Docker Compose
```docker compose up --build```

### Files with tasks

```
    home_work_core.ipynb
    home_work_extra_task.ipynb.ipynb
```

